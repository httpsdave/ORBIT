<script setup>
import { Link } from '@inertiajs/vue3';

defineProps({
  links: Array,
});
</script>

<template>
  <div v-if="links.length > 3" class="flex flex-wrap -mb-1">
    <template v-for="(link, key) in links" :key="key">
      <div v-if="link.url === null" 
           class="mr-1 mb-1 px-4 py-2 text-sm text-gray-400 border rounded"
           :class="{ 'ml-auto': link.label === 'Next' }">
        {{ link.label }}
      </div>
      <Link v-else
            class="mr-1 mb-1 px-4 py-2 text-sm border rounded hover:bg-gray-100 focus:outline-none focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-25"
            :class="{
              'bg-blue-600 text-white hover:bg-blue-700': link.active,
              'ml-auto': link.label === 'Next'
            }"
            :href="link.url">
        {{ link.label }}
      </Link>
    </template>
  </div>
</template>