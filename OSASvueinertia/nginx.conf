user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 50M;

    # Enable HTTP/2 if available
    http2 on;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/x-javascript application/xml application/xml+rss application/rss+xml font/truetype font/opentype application/vnd.ms-fontobject image/svg+xml;
    
    # Enable serving pre-compressed .gz files
    gzip_static on;

    server {
        listen ${PORT:-8000};
        server_name _;
        root /app/public;
        index index.php;

        charset utf-8;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # Cache headers for static assets - Images with aggressive caching
        location ~* \.(jpg|jpeg|png|gif|webp|svg|ico)$ {
            expires 1y;
            add_header Cache-Control "public, max-age=31536000, immutable";
            add_header X-Content-Type-Options "nosniff";
            access_log off;
            # Enable TCP optimizations
            tcp_nodelay on;
            tcp_nopush on;
        }

        location ~* \.(js|css|woff|woff2|ttf|otf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, max-age=31536000, immutable";
            add_header X-Content-Type-Options "nosniff";
            access_log off;
            # Enable TCP optimizations
            tcp_nodelay on;
            tcp_nopush on;
        }

        # Vite build assets with strong caching
        location ^~ /build/ {
            expires 1y;
            add_header Cache-Control "public, max-age=31536000, immutable";
            add_header X-Content-Type-Options "nosniff";
            access_log off;
            # Enable TCP optimizations
            tcp_nodelay on;
            tcp_nopush on;
        }
        
        # Images directory with optimized serving
        location ^~ /images/ {
            expires 1y;
            add_header Cache-Control "public, max-age=31536000, immutable";
            add_header X-Content-Type-Options "nosniff";
            access_log off;
            # Enable TCP optimizations for faster delivery
            tcp_nodelay on;
            tcp_nopush on;
            # Optional: Enable direct I/O for large files
            directio 4m;
            directio_alignment 512;
        }

        # Main location block
        location / {
            try_files $uri $uri/ /index.php?$query_string;
            
            # Add Link header for critical resource preloading
            add_header Link "</images/LSPU9.jpg>; rel=preload; as=image; fetchpriority=high" always;
        }

        location = /favicon.ico { access_log off; log_not_found off; }
        location = /robots.txt  { access_log off; log_not_found off; }

        # Deny access to hidden files
        location ~ /\. {
            deny all;
        }

        # PHP-FPM Configuration
        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 300;
            fastcgi_buffers 16 16k;
            fastcgi_buffer_size 32k;
        }
    }
}
